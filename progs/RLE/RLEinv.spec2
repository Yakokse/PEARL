// Either can be ignored for interesting PE

chars = '((nil . (nil . (nil . nil))) .
         ((nil . (nil . nil)) .
         ((nil . nil) .
         (nil .
         nil))))
counts = '(1.(3.(2.(1.nil))))

// For generalizing if need be
// Needed when only specializing on chars, e.g. ignore counts + text
text = 'nil
